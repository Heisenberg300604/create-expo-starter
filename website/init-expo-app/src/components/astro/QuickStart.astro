---
import { TerminalCommand } from "../react/TerminalCommand";
---

<section id="docs" class="py-20 lg:py-28 bg-secondary/30">
    <div class="section-container">
        <div class="max-w-2xl mx-auto">
            <div class="text-center mb-8">
                <h2 class="text-2xl sm:text-3xl font-bold mb-3">Quick start</h2>
                <p class="text-muted-foreground">
                    Get up and running in under a minute.
                </p>
            </div>

            <div
                class="w-full max-w-2xl mx-auto border border-border bg-[#0c0c0c] rounded-xl overflow-hidden shadow-2xl"
            >
                <div
                    class="border-b border-white/10 bg-[#1a1a1a] p-4 flex items-center"
                >
                    <div class="flex gap-2">
                        <div class="w-3 h-3 rounded-full bg-[#ff5f56]"></div>
                        <div class="w-3 h-3 rounded-full bg-[#ffbd2e]"></div>
                        <div class="w-3 h-3 rounded-full bg-[#27c93f]"></div>
                    </div>
                    <div class="ml-4 text-xs text-muted-foreground font-mono">
                        bash — 80x24
                    </div>
                </div>
                <div class="p-6 space-y-8 font-mono text-sm">
                    <div>
                        <div
                            class="flex items-center gap-2 text-muted-foreground mb-2"
                        >
                            <span class="text-green-500">➜</span>
                            <span>~</span>
                            <span>Create a new app</span>
                        </div>
                        <TerminalCommand
                            command="npx init-expo-app my-app"
                            client:visible
                        />
                    </div>

                    <div>
                        <div
                            class="flex items-center gap-2 text-muted-foreground mb-2"
                        >
                            <span class="text-green-500">➜</span>
                            <span>~</span>
                            <span>Navigate to your app</span>
                        </div>
                        <TerminalCommand command="cd my-app" client:visible />
                    </div>

                    <div>
                        <div
                            class="flex items-center gap-2 text-muted-foreground mb-2"
                        >
                            <span class="text-green-500">➜</span>
                            <span>~</span>
                            <span>Start the development server</span>
                        </div>
                        <TerminalCommand
                            command="npx expo start"
                            client:visible
                        />
                    </div>
                </div>
            </div>

            <div class="mt-8 p-4 rounded-lg border border-border bg-card">
                <p class="text-sm text-muted-foreground">
                    <span class="font-medium text-foreground"
                        >Need more info?</span
                    >{" "}
                    Check out the full documentation on{" "}
                    <a
                        href="https://github.com"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="text-accent hover:underline"
                    >
                        GitHub
                    </a>.
                </p>
            </div>
        </div>
    </div>
</section>
